# KOVCHEG Blog 3.6.0 migration

The migration is non-destructive for content:

- rows with `type=portfolio` become `type=page`;
- category links are removed from Pages;
- entry text, media paths, URLs, revisions and metadata remain stored.
