<div class="page-head"><div><h1>Модули</h1><p>Установка независимых функций без изменения файлов ядра.</p></div></div>
<section class="panel" style="margin-bottom:20px"><h2>Установить модуль формата 2</h2><p>Пакет должен содержать <code>manifest.json</code>, <code>bootstrap.php</code>, а PHP-классы — только в <code>src</code>, маршруты — в <code>routes</code>, шаблоны — в <code>views</code>.</p><form method="post" enctype="multipart/form-data" action="<?=e(app_url('/studio/modules/install'))?>"><?=csrf_field()?><div class="form-grid"><div class="field"><label>ZIP-пакет</label><input type="file" name="package" accept="application/zip,.zip" required></div><label class="check-row" style="align-self:end"><input type="checkbox" name="enable" value="1" checked> Включить после установки</label></div><button class="button primary">Проверить и установить</button></form></section>
<?php if($modules):?><div class="module-grid"><?php foreach($modules as $module):?><article class="module-card"><div style="display:flex;justify-content:space-between;gap:12px"><span class="module-status <?=empty($module['files_ok'])?'broken':''?>"><?=e((string)$module['health'])?></span><small>v<?=e((string)$module['version'])?></small></div><h3><?=e((string)$module['name'])?></h3><p><?=e((string)($module['description']??''))?></p><small><code><?=e((string)$module['slug'])?></code> · формат <?=(int)($module['package_format']??1)?></small><div class="user-actions" style="margin-top:16px"><form method="post" action="<?=e(app_url('/studio/modules/'.rawurlencode((string)$module['slug']).'/toggle'))?>"><?=csrf_field()?><input type="hidden" name="enabled" value="<?=!empty($module['enabled'])?'0':'1'?>"><button class="button small"><?=!empty($module['enabled'])?'Отключить':'Включить'?></button></form><form method="post" data-confirm="Удалить модуль и его файлы? Данные таблиц модуля автоматически не удаляются." action="<?=e(app_url('/studio/modules/'.rawurlencode((string)$module['slug']).'/remove'))?>"><?=csrf_field()?><button class="button small danger">Удалить</button></form></div></article><?php endforeach;?></div><?php else:?><div class="empty-state"><h2>Модули пока не установлены</h2><p>Ядро готово принимать независимые расширения формата 2.</p></div><?php endif;?>
