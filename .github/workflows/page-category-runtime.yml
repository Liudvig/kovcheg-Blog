name: KOVCHEG Pages runtime checks

on:
  push:
    branches: [main, feature/page-category-core-3.7.0]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  runtime:
    runs-on: ubuntu-latest
    services:
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: root
          MARIADB_DATABASE: kovcheg
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
          extensions: pdo_mysql, mbstring, dom, fileinfo
      - name: Runtime config
        run: |
          mkdir -p config storage/cache storage/logs storage/uploads storage/backups storage/builds
          chmod -R 777 storage
          cat > config/config.php <<'PHP'
          <?php
          return [
            'app'=>[
              'name'=>'KOVCHEG CMS CI',
              'url'=>'http://127.0.0.1:8099',
              'base_path'=>'',
              'timezone'=>'UTC',
              'debug'=>true,
              'key'=>'base64:'.base64_encode(str_repeat('k',32)),
            ],
            'database'=>[
              'driver'=>'mysql',
              'host'=>'127.0.0.1',
              'port'=>3306,
              'name'=>'kovcheg',
              'user'=>'root',
              'password'=>'root',
              'charset'=>'utf8mb4',
            ],
          ];
          PHP
      - name: Initialize schema
        run: |
          php -r '$schema=require "database/schema.php"; if(!is_string($schema)||trim($schema)==="") exit(1); file_put_contents("/tmp/kovcheg-schema.sql",$schema);'
          MYSQL_PWD=root mysql --protocol=tcp -h 127.0.0.1 -P 3306 -u root kovcheg < /tmp/kovcheg-schema.sql
      - name: Apply migrations
        run: |
          set -euo pipefail
          for migration in $(find migrations -maxdepth 1 -type f -name '*.sql' | sort); do
            echo "Applying ${migration}"
            MYSQL_PWD=root mysql --protocol=tcp -h 127.0.0.1 -P 3306 -u root kovcheg < "$migration"
          done
      - name: Seed runtime
        run: |
          php -r '
            $_SERVER["SCRIPT_NAME"]="cli";
            require "app/bootstrap.php";
            \Kovcheg\DB::run("INSERT INTO users (username,email,password_hash,display_name,role,approval_status,is_active,created_at,updated_at) VALUES (?,?,?,?,\"owner\",\"approved\",1,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)",["owner","owner@example.test",password_hash("password",PASSWORD_DEFAULT),"Owner"]);
            foreach(["site_name"=>"KOVCHEG CMS CI","blog_theme"=>"kovcheg-portal","search_indexing"=>"0"] as $key=>$value){
              \Kovcheg\DB::run("INSERT INTO settings (setting_key,setting_value,updated_at) VALUES (?,?,CURRENT_TIMESTAMP) ON DUPLICATE KEY UPDATE setting_value=VALUES(setting_value),updated_at=CURRENT_TIMESTAMP",[$key,$value]);
            }
          '
      - name: Repair Portal layout
        run: |
          php -r '
            $_SERVER["SCRIPT_NAME"]="cli";
            require "app/bootstrap.php";
            require_once "app/BlogLayoutRepair.php";
            \Kovcheg\Blog\LayoutRepair::ensure();
          '
      - name: Start PHP server
        run: |
          php -S 127.0.0.1:8099 index.php > /tmp/kovcheg-runtime.log 2>&1 &
          echo $! > /tmp/kovcheg-runtime.pid
          for attempt in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:8099/ >/dev/null; then break; fi
            sleep 1
          done
      - name: Home route
        run: |
          curl --fail-with-body -sS -D /tmp/home.headers http://127.0.0.1:8099/ -o /tmp/home.html
          grep -q 'data-layout-zone="matrix.header.3"' /tmp/home.html
          grep -q 'portal-matrix-sidebar portal-matrix-sidebar--left' /tmp/home.html
          grep -q 'portal-matrix-sidebar portal-matrix-sidebar--right' /tmp/home.html
          grep -q 'portal-matrix-footer-grid' /tmp/home.html
          grep -q 'Ланцет Семён Борисович' /tmp/home.html
      - name: Legacy Blog alias
        run: curl --fail-with-body -sS -D /tmp/blog.headers http://127.0.0.1:8099/blog -o /tmp/blog.html
      - name: Legacy Portfolio alias
        run: curl --fail-with-body -sS -D /tmp/portfolio.headers http://127.0.0.1:8099/portfolio -o /tmp/portfolio.html
      - name: Studio route
        run: curl --fail-with-body -sS -D /tmp/studio.headers http://127.0.0.1:8099/studio -o /tmp/studio.html
      - name: Appearance route
        run: curl --fail-with-body -sS -D /tmp/appearance.headers http://127.0.0.1:8099/appearance -o /tmp/appearance.html
      - name: Runtime log
        if: always()
        run: cat /tmp/kovcheg-runtime.log || true
